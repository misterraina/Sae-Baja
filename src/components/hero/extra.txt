      <div className="w-full overflow-hidden relative mt-8 ">
        <div className="flex animate-marquee-left space-x-6">
          {[ph1, ph2, ph3, ph8, ph9, ph7].map((image, index) => (
            <img
              key={index}
              src={image}
              alt={`Team ${index}`}
              className="w-2/6 h-96 object-cover hover:scale-110 transition duration-300 rounded-lg"
            />
          ))}
          {/* Duplicate the images for seamless looping */}
          {[ph1, ph2, ph3, ph8, ph9, ph7].map((image, index) => (
            <img
              key={`dup-${index}`}
              src={image}
              alt={`Duplicate Team ${index}`}
              className="w-2/6 h-96 object-cover hover:scale-110 transition duration-300 rounded-lg"
            />
          ))}
        </div>
      </div>



   <div className="w-full overflow-hidden relative mt-6">
        <div className="flex animate-marquee-right space-x-6">
          {[ph4, ph5, ph6, ph0, ph10, ph11].map((image, index) => (
            <img
              key={index}
              src={image}
              alt={`Vehicle ${index}`}
              className="w-1/3 h-64 object-cover hover:scale-110 transition duration-300"
            />
          ))}
          {/* Duplicate the images for seamless looping */}
          {[ph4, ph5, ph6, ph0, ph10, ph11, ph12].map((image, index) => (
            <img
              key={`dup-${index}`}
              src={image}
              alt={`Duplicate Vehicle ${index}`}
              className="w-1/3 h-64 object-cover hover:scale-110 transition duration-300"
            />
          ))}
        </div>
      </div>


{/* Row 1: Upper row moving right to left */}
      <div className="w-full overflow-hidden relative ">
        <div className="flex animate-marquee-left space-x-6">
          {[ph1, ph2, ph3, ph8, ph9, ph7].map((image, index) => (
            <img
              key={index}
              src={image}
              alt={`Team ${index}`}
              className="w-2/6 h-96 object-cover hover:scale-110 transition duration-300 rounded-lg"
            />
          ))}
          {/* Duplicate the images for seamless looping */}
          {[ph1, ph2, ph3, ph8, ph9, ph7].map((image, index) => (
            <img
              key={`dup-${index}`}
              src={image}
              alt={`Duplicate Team ${index}`}
              className="w-2/6 h-96 object-cover hover:scale-110 transition duration-300 rounded-lg"
            />
          ))}
        </div>
      </div>